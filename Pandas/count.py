# count category number in a column
X_train[col].nunique()

# count the value in a column
df.col1.value_counts()


# count() count the value without nan and return the dataframe
# size() count nan value and return the series
